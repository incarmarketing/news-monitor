# ?? Cron?? GitHub Actions ????

GitHub Actions? `schedule`? ??? ? ????, ???? ? ??? ??? ?? cron ???? GitHub workflow? ?? ??? ???? ?????. ?? `Negative Article Watch`? GitHub ???? ?? ??? 5??? ????, 1?? ??? ?? ??? ?? ?????. ??? ??? Supabase? ??? ?? ?? ?? ??? ???? ?? ?????. cron-job.org? ? ??? ? ? ? ??? ?? ????, Supabase Cron? cron-job.org?? ??? ? DB ???? ????? ?? ??? ???? ?????.

## 1. GitHub ?? ???

1. GitHub ?? ?? ??? > Settings
2. Developer settings > Personal access tokens > Fine-grained tokens
3. Generate new token
4. Repository access: `incarmarketing/news-monitor`? ??
5. Permissions
   - Actions: Read and write
   - Contents: Read-only
6. ??? ??? ?????.

??: ? ??? ?? cron ???? ?????, ?? ? ???? ?? ???? ???.

## 1-1. ?????? ?? ????

?? PC? ?? ?? ???? ???? GitHub ???? Settings > Secrets and variables > Actions? ?? ? ?? ?????.

- `CRONJOB_API_KEY`: cron-job.org API key
- `CRON_DISPATCH_TOKEN`: GitHub fine-grained token

? ?? GitHub Actions > `Sync External Cron` > Run workflow? ?????. ? ????? GitHub ???? ???? `setup_cronjob_org.py`? ????? ?? PC? ?? ???? ???? cron-job.org ??? ??/?????. ???? ?? ? ? ?? ??? ?? ?????.

???? ???? ???? ? ?? ???? ?????.

```powershell
$env:CRONJOB_API_KEY="cron-job.org API key"
$env:CRON_DISPATCH_TOKEN="GitHub fine-grained token"
python setup_cronjob_org.py
```

? ????? ?? ??? ?? ?????, ?? ??? ??? ?? ??? ???????.

- `news-monitor negative watch`
- `news-monitor daily 08`
- `news-monitor daily 13`
- `news-monitor daily 18`
- `news-monitor weekly report`
- `news-monitor monthly report`

cron-job.org API key? cron-job.org Console > Settings?? ?????. cron-job.org ?? ??? ??? API? `Authorization: Bearer <API_KEY>` ???? ????, ?? payload? JSON?? ????.

## 2. ???? 24?? 5? ?? ??

?? cron ????? ?? ??? 24?? ?? 5??? ??? ? ????. ?? GitHub Actions? ?? ???? 5??? ????, ?? ??? ??? ?????. hourly schedule? ??? ??? ? ???? ???????. `negative_watch.py`? ????? 24/7? ???? DB?? ?? `minutes_back=5`? ?????. ?, ?? ??? ???? ??? ?? ?? ??? ???? ?? `minutes_back`? ???? ?? ? ????.

- Method: `POST`
- URL:

```text
https://api.github.com/repos/incarmarketing/news-monitor/actions/workflows/negative-watch.yml/dispatches
```

- Headers:

```text
Accept: application/vnd.github+json
Authorization: Bearer <GITHUB_TOKEN>
X-GitHub-Api-Version: 2022-11-28
Content-Type: application/json
```

- Body:

```json
{"ref":"main"}
```

?? cron:

```text
*/5 * * * *
```

`setup_cronjob_org.py`? ???? `news-monitor negative watch` ??? cron-job.org?? ?? 00:00~23:59 KST, 5? ??? ??/???????. ?? ??? `check_cronjob_org.py`? minutes ?? `[0, 5, 10, ..., 55]`, hours ?? `[0, 1, ..., 23]`?? ?????.

????? `?? ?? 5?`? ? ? ??? ? ? ? ? ???? ????? ?????. `?? ??? ?? ??`? ??? Supabase `negative_watch_runs`? ?? ??? 12? ?? ??? ?????. ? ?? GitHub Actions? `Negative Article Watch` ?? ??? ?? ????, GitHub Secrets? `CRONJOB_API_KEY`, `CRON_DISPATCH_TOKEN`? ??? `Sync External Cron`? ?? ?????. ??? Supabase `cron.job`? `news-monitor-supabase-watchdog` ??? active?? ?????.

## 2-1. Supabase Cron ?? ??

Supabase Cron? `pg_cron`? `pg_net`?? Edge Function? ?????. ? ???? ?? ?? ??? `news-monitor-supabase-watchdog`?? 5??? ?????.

- ?? ??: `trigger-news-collection` Edge Function? `watchdog` ??
- ?? ??: `*/5 * * * *`
- ??? ??: Supabase Vault? `news_monitor_project_url`, `news_monitor_publishable_key`
- ?? ??: ?? ???, ??/?? ???, ???? ??? ??? ???? GitHub Actions? ?? dispatch

?? ???? ??????? ?????? ?? ?? Supabase Vault? ?? ?? ?????. ? ??? ???? ???? ????.

```sql
select vault.create_secret('<SUPABASE_PROJECT_URL>', 'news_monitor_project_url');
select vault.create_secret('<SUPABASE_PUBLISHABLE_KEY>', 'news_monitor_publishable_key');
```

??/?? SQL? `supabase/migrations/20260531070408_supabase_watchdog_cron.sql`? ????. ?? ? ?? ??? ??? ????.

```sql
select jobid, jobname, schedule, active
from cron.job
where jobname = 'news-monitor-supabase-watchdog';

select jobid, runid, status, start_time, end_time
from cron.job_run_details
where jobid = (
  select jobid from cron.job where jobname = 'news-monitor-supabase-watchdog'
)
order by start_time desc
limit 5;
```

## 3. ?? ??? ??

?? cron ????? ?? URL? ?????.

- Method: `POST`
- URL:

```text
https://api.github.com/repos/incarmarketing/news-monitor/actions/workflows/news-briefing.yml/dispatches
```

- Headers? ?? ?????.

### 08:00 ???

```json
{
  "ref": "main",
  "inputs": {
    "period_reports": "none",
    "send_kakao": "true",
    "report_slot": "08"
  }
}
```

### 13:00 ???

```json
{
  "ref": "main",
  "inputs": {
    "period_reports": "none",
    "send_kakao": "true",
    "report_slot": "13"
  }
}
```

### 18:00 ???

```json
{
  "ref": "main",
  "inputs": {
    "period_reports": "none",
    "send_kakao": "true",
    "report_slot": "18"
  }
}
```

?? cron:

```text
0 23 * * 0-4   # KST 08:00, UTC 23:00 ??
0 4 * * 1-5    # KST 13:00
0 9 * * 1-5    # KST 18:00
```

?? cron ???? KST ???? ???? ?? `08:00`, `13:00`, `18:00` ??? ???? ???.

## 4. ??/?? ??? ??

?? ???? ?? ??? 07:00 KST, ?? ???? ?? 1? 07:00 KST? ?????.

### ?? ???

```json
{
  "ref": "main",
  "inputs": {
    "period_reports": "weekly",
    "send_kakao": "true",
    "report_slot": "auto"
  }
}
```

### ?? ???

```json
{
  "ref": "main",
  "inputs": {
    "period_reports": "monthly",
    "send_kakao": "true",
    "report_slot": "auto"
  }
}
```

## 5. ?? ?? ??

- GitHub Actions `schedule`? ???? ?????.
- ?? cron? ? ???? ?????.
- ?? cron? ???? GitHub ?? schedule? ? ? ? ?????.
- ?? ??? `.run-state`, `.watch-state`, Supabase `notification_sends.dedupe_key` ???? ????.
- ?? ? ???? ?? ??? ?? `news-briefing.yml`? `period_reports=monthly`, `report_month=YYYY-MM` ??? ?????.

## 6. ??? ?? ?? cron ???

- cron-job.org: ??? ???? ?? HTTP POST ?? ??
- EasyCron: ??? ????? ?? ?? ?? ??
- UptimeRobot: ?? ping/HTTP ??? ??
- Cronitor: ????? ???? ???? ?? ? ??

???? `cron-job.org`? ?????.
