name: AI News Briefing

on:
  workflow_dispatch:
    inputs:
      period_reports:
        description: "Generate period report preview"
        required: true
        default: "both"
        type: choice
        options:
          - none
          - weekly
          - monthly
          - both
      send_kakao:
        description: "Send KakaoTalk notification"
        required: true
        default: false
        type: boolean
  push:
    branches:
      - main
    paths-ignore:
      - ".run-state/**"
      - "data/daily/**"
  schedule:
    - cron: "0 22 * * *" # 07:00 KST weekly/monthly
    - cron: "10 22 * * *" # 07:10 KST weekly/monthly backup
    - cron: "20 22 * * *" # 07:20 KST weekly/monthly backup
    - cron: "30 22 * * *" # 07:30 KST weekly/monthly backup
    - cron: "5 23 * * *" # 08:05 KST
    - cron: "15 23 * * *" # 08:15 KST backup
    - cron: "5 4 * * *"  # 13:05 KST
    - cron: "15 4 * * *"  # 13:15 KST backup
    - cron: "5 9 * * *"  # 18:05 KST
    - cron: "15 9 * * *"  # 18:15 KST backup

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build-and-send:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check schedule slot
        id: guard
        run: python schedule_guard.py begin
        env:
          SCHEDULE_CRON: ${{ github.event.schedule }}

      - name: Install dependencies
        if: steps.guard.outputs.should_run == 'true'
        run: python -m pip install -r requirements.txt

      - name: Generate briefing
        if: steps.guard.outputs.should_run == 'true'
        run: python run_once.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}

      - name: Generate period reports
        if: steps.guard.outputs.should_run == 'true' && (steps.guard.outputs.should_period == 'true' || github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.event.inputs.period_reports != 'none'))
        run: |
          PERIOD="${{ github.event.inputs.period_reports }}"
          KST_WEEKDAY="$(TZ=Asia/Seoul date +%u)"
          KST_DAY="$(TZ=Asia/Seoul date +%d)"

          if [ "${{ steps.guard.outputs.should_period }}" = "true" ]; then
            if [ "$KST_WEEKDAY" = "1" ]; then
              python period_report.py weekly_previous
            else
              echo "Weekly report is only generated on Monday KST."
            fi
            if [ "$KST_DAY" = "01" ]; then
              python period_report.py monthly_previous
            else
              echo "Monthly report is only generated on the 1st KST."
            fi
            exit 0
          fi

          if [ "${{ github.event_name }}" = "push" ] || [ "$PERIOD" = "weekly" ] || [ "$PERIOD" = "both" ]; then
            python period_report.py weekly
          fi
          if [ "${{ github.event_name }}" = "push" ] || [ "$PERIOD" = "monthly" ] || [ "$PERIOD" = "both" ]; then
            python period_report.py monthly
          fi
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Prepare static report
        if: steps.guard.outputs.should_run == 'true'
        run: python publish_report.py

      - name: Configure GitHub Pages
        if: steps.guard.outputs.should_run == 'true'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: steps.guard.outputs.should_run == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy to GitHub Pages
        if: steps.guard.outputs.should_run == 'true'
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Send KakaoTalk link
        if: steps.guard.outputs.should_run == 'true' && ((github.event_name == 'schedule' && steps.guard.outputs.should_period != 'true') || github.event.inputs.send_kakao == 'true' || (github.event_name == 'push' && contains(github.event.head_commit.message, '[send-kakao]')))
        run: python kakao_report_send.py
        env:
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
          KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
          REPORT_PUBLIC_URL: ${{ steps.deployment.outputs.page_url }}

      - name: Send period KakaoTalk link
        if: steps.guard.outputs.should_run == 'true' && steps.guard.outputs.should_period == 'true'
        run: |
          if [ "$(TZ=Asia/Seoul date +%u)" = "1" ]; then
            python kakao_period_send.py weekly
          fi
          if [ "$(TZ=Asia/Seoul date +%d)" = "01" ]; then
            python kakao_period_send.py monthly
          fi
        env:
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
          KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
          REPORT_PUBLIC_URL: ${{ steps.deployment.outputs.page_url }}

      - name: Persist run archive
        if: steps.guard.outputs.should_run == 'true'
        run: |
          if [ "${{ steps.guard.outputs.should_mark }}" = "true" ]; then
            python schedule_guard.py mark "${{ steps.guard.outputs.marker_path }}"
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f data/daily
          if [ -d period_reports ]; then
            git add -f period_reports
          fi
          if [ -d .run-state ]; then
            git add .run-state
          fi
          if git diff --cached --quiet; then
            echo "No archive changes to commit."
            exit 0
          fi
          git commit -m "Archive briefing data"
          git pull --rebase
          git push
